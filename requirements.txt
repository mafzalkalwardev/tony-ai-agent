# Optional Python dependencies for TONY bridges
# Install: pip install -r requirements.txt

# Desktop automation bridge (scripts/tony-automation.py)
# Used by desktop_automate tool — click, type, hotkey, screenshot
pyautogui>=0.9.54
pyperclip>=1.9.0
Pillow>=10.0.0

# Presentation creation (scripts/tony-presentation.py)
python-pptx>=1.0.0

# Original tony-ai desktop assistant (integrations/repos/tony-ai after npm run integrations:init)
# See skills/tony-desktop/SKILL.md — install inside that repo:
#   cd integrations/repos/tony-ai && pip install -r requirements.txt
