---
name: paul-builder
description: Ship integrations and local deployments with Paul, TONY's builder specialist
---

# Paul Builder Skill

Use when the user asks to **build**, **ship**, **integrate**, or **wire** systems locally.

## Workflow

1. Query graphify for related files and dependencies
2. Read existing code before editing — match conventions
3. Use `paul_build` for multi-step builder tasks
4. Run `npm test` after changes
5. Report what shipped and how to run it locally

## Paul focuses on

- Groq brain wiring
- Deepgram + ElevenLabs voice lane
- Graphify knowledge graph
- Obsidian agentic brain vault
- Charlie OS local boot

## Commands

```bash
npm run charlie          # Boot Charlie OS + gateway
npm run charlie:build -- "Wire voice endpoints"
npm run graph            # Rebuild knowledge graph
```
